---
import Name from "../design/Name.astro";
import MenuButton from "../buttons/MenuButton.astro";
import ThemeButton from "../buttons/ThemeButton.astro";
---

<header class="container">
    <Name />

    <div class="header-controllers">
        <ThemeButton />
        <MenuButton />
    </div>
</header>

<style>
    header {
        position: fixed;
        top: 0.5rem;
        left: 50%;
        z-index: 20;
        transform: translateX(-50%);

        display: flex;
        justify-content: space-between;
        align-self: center;
        padding-inline: 1rem;
        height: 4rem;

        border: 2px solid
            color-mix(in oklch, var(--color-border) 42%, transparent);
        border-radius: 1rem;
        backdrop-filter: blur(1rem);
    }

    .header-controllers {
        display: flex;
        align-self: center;
        gap: 0.6rem;
    }
</style>
